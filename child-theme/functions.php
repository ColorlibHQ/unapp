<?php
/**
 * Unapp Child.
 *
 * WordPress loads a block child theme's style.css and theme.json automatically,
 * so this file exists for PHP customisations only. It is safe to delete if you
 * do not need any.
 *
 * @package Unapp_Child
 */

defined( 'ABSPATH' ) || exit;

/**
 * Example: add your own starter site.
 *
 * See docs/extending.md in the parent theme for the full shape of a definition.
 */
// add_filter( 'unapp_starter_sites', function ( $sites ) {
// return $sites;
// } );
